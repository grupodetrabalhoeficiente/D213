/*const verificar = document.getElementById("submit");
verificar.onclick = Submeter;

function Submeter() {
    if (document.getElementById("NotaTexto").value.length === 0) {
        alert("Nota vazia!");
    }
}*/
